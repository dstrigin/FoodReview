<!DOCTYPE html>
<html>
<head>
  <title>FoodReview</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <style>
    body{
        background-color: #ffe5d4;
    }
  </style>
</head>
<body>
<div class="container mt-5">
  <!-- Header -->
  <header class="text-center mb-5">
    <h1 class="display-4">FoodReview</h1>
    <p class="lead text-muted">Обзоры на заведения Ростова-на-Дону</p>
  </header>

  <!-- List of Places -->
  <section>
    <h2 class="h4 mb-4">Заведения</h2>

    <% if @places.any? %>
      <ul class="list-group">
        <% @places.each do |place| %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <%= link_to place.name, place_path(place), class: "text-decoration-none" %>
            <span class="badge bg-primary">#{place.id}</span>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-muted">Заведений пока нет. Добавьте новые заведения!</p>
    <% end %>

    <!-- Add New Place Button -->
    <div class="mt-4 text-end">
      <%= link_to 'Добавить заведение', new_place_path, class: "btn btn-success" %>
    </div>
  </section>
</div>
</body>
</html>
